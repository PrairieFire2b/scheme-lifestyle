{
	"type": "module",
	"devDependencies": {
		"micromark": "^4.0.2",
		"micromark-extension-frontmatter": "^2.0.0",
		"micromark-extension-gfm": "^3.0.0"
	},
	"scripts": {
		"build": "node build.js",
		"dev": "nodemon --watch src/* --watch build.js --exec \"npm run build\"",
		"serve": "serve dist"
	},
	"dependencies": {
		"micromark-extension-directive": "^4.0.0"
	}
}
